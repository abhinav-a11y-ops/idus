interface:
  display_name: "Idus"
  short_description: "Repo-specific patterns and workflows for idus"
  default_prompt: "Use the idus repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true